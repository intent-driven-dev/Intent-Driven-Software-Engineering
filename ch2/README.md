# Chapter 2 Listings

This folder contains the Chapter 2 code listings as Git submodules:

- `vibey-url-shortener`
- `spec-driven-url-shortener`

Each submodule is an independent Git repository. To understand the progression of the code, go inside a submodule folder and review its Git history directly:

```sh
cd vibey-url-shortener
git log --oneline
```

Use the same approach for `spec-driven-url-shortener`.
